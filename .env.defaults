# =============================================================================
# MS Elevate LEAPS Tracker - Repository Default Environment Values
# =============================================================================
# This file contains default values that are safe to commit to version control.
# It is the base layer for the three-tier environment management strategy:
#   1. Repository Defaults (this file) - committed to git
#   2. Environment Specific (/.env.development, /.env.staging, /.env.production)
#   3. Local Overrides (/.env.local) - gitignored
#
# DO NOT put sensitive values in this file - use environment-specific files or .env.local
# =============================================================================

# =============================================================================
# APPLICATION SETTINGS - Safe defaults for all environments
# =============================================================================

# Node environment - will be overridden by environment-specific files
NODE_ENV=development

# Rate limiting defaults - safe for all environments
RATE_LIMIT_RPM=60
WEBHOOK_RATE_LIMIT_RPM=120

# Database connection pool settings
DATABASE_POOL_MAX=10
DATABASE_POOL_TIMEOUT=30000

# Debug logging - disabled by default
DEBUG=false

# =============================================================================
# EMAIL TEMPLATES - Static configuration
# =============================================================================

# Email templates - these can be in defaults as they're not sensitive
FROM_EMAIL=MS Elevate <noreply@leaps.mereka.org>
REPLY_TO_EMAIL=support@leaps.mereka.org

# =============================================================================
# DEVELOPMENT SEEDING - Safe test values only
# =============================================================================

# Default seed values for development - safe to commit
SEED_ADMIN_ID=demo-admin-1
SEED_ADMIN_EMAIL=admin@leaps.mereka.org
SEED_ADMIN_NAME=Admin User
SEED_ADMIN_HANDLE=admin_user

# =============================================================================
# PLACEHOLDER VALUES - Must be overridden in environment-specific files
# =============================================================================

# Database - MUST be set in environment-specific files
DATABASE_URL=postgresql://postgres:password@localhost:5432/elevate_leaps
DIRECT_URL=postgresql://postgres:password@localhost:5432/elevate_leaps

# Authentication - MUST be set in environment-specific files
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_placeholder
CLERK_SECRET_KEY=sk_test_placeholder
CLERK_WEBHOOK_SECRET=whsec_placeholder

# File Storage - MUST be set in environment-specific files
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Kajabi Integration - MUST be set in environment-specific files
KAJABI_WEBHOOK_SECRET=your-secure-webhook-secret
KAJABI_API_KEY=your-kajabi-api-key
KAJABI_CLIENT_SECRET=your-kajabi-client-secret

# Application URLs - will be overridden by environment-specific files
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Email Service - MUST be set in environment-specific files if used
RESEND_API_KEY=re_placeholder

# Optional Services - Placeholders
OPENAI_API_KEY=sk-placeholder
CRON_SECRET=placeholder-cron-secret
SENTRY_DSN=https://placeholder@sentry.io/project-id
ANALYTICS_API_KEY=placeholder-analytics-key

# Turbo Remote Cache - Optional
TURBO_TOKEN=placeholder-turbo-token

# =============================================================================
# VERCEL-SPECIFIC VARIABLES - Auto-populated in deployment
# =============================================================================
# These are typically set automatically by Vercel, included for completeness
# VERCEL=1
# VERCEL_ENV=development
# VERCEL_URL=your-app.vercel.app
# VERCEL_GIT_COMMIT_SHA=commit-sha