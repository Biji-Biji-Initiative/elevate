// Root Vitest config to ensure Vitest picks up settings without --config
// Delegates to the real config under tests/vitest.config.ts
import config from './tests/vitest.config'
export default config

